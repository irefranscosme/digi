'use client';

import { Flex, Text } from '@chakra-ui/react';
import React from 'react';

const Dashboard = () => {
    return (
        <Flex>
            <Text>Dashboard</Text>
        </Flex>
    );
};

export default Dashboard;
