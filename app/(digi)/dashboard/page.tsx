import Dashboard from '@/components/dashboard/dashboard';
import React from 'react';

export const metadata = {
    title: 'DIGI | Dashboard',
    description: '',
};

const Page = () => {
    return <Dashboard />;
};

export default Page;
